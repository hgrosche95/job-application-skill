# Wöchentliche Update-PRs statt Sicherheitslücken erst beim nächsten Review zu
# finden. Minor- und Patch-Updates kommen gebündelt als ein PR pro Ökosystem,
# Major-Updates einzeln, weil die meist Code-Änderungen brauchen.
version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    groups:
      # NestJS-Pakete müssen dieselbe Hauptversion haben (platform-express 12
      # mit core 11 startet nicht, siehe job-application-skill#14). Minor/Patch
      # kommen gebündelt; Majors ignoriert Dependabot (siehe ignore unten), weil
      # es sie trotz Gruppe einzeln vorgeschlagen hat. NestJS-Majors von Hand.
      nestjs:
        patterns: ["@nestjs/*"]
      npm-minor-patch:
        update-types: [minor, patch]
    ignore:
      - dependency-name: "@nestjs/*"
        update-types: ["version-update:semver-major"]

  - package-ecosystem: pip
    directory: /bewerbungshelfer
    schedule:
      interval: weekly

  - package-ecosystem: docker
    directory: /apps/api
    schedule:
      interval: weekly

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ["*"]
